= div_for comment do
  .del
    = link_to image_tag("web-app-theme/icons/cross.png", alt: 'Delete'), event_comment_path(comment.event, comment, format: :json),
      method: "delete", remote: true, class: "button", confirm: t("web-app-theme.confirm", default: "Are you sure?")
  %span.author= comment.author
  %em on #{comment.created_at.strftime('%b %d, %Y at %I:%M %p')}
  = simple_format comment.body
